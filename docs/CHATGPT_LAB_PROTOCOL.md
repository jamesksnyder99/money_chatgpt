# money_chatgpt — research and verification protocol

This is an independent laboratory seeded from `jamesksnyder99/money`. Source provenance is historical evidence, not permission to access the original local repository.

## Roles and workflow

ChatGPT is research director and auditor. Opus in Claude Code is the executor for **CG Arrow 014**. Detailed assignment: `docs/CG_BUILD_ARROW_014.md`.

1. Pull current `main` without overwriting legitimate local work; confirm root/remote.
2. Read `AGENTS.md`, `docs/SUCCESS.md`, `CLAUDE.md`, and the active arrow.
3. Execute only the active scope, preserve prior evidence, test, inspect public safety, commit/push reviewed outputs.
4. Verify remote equality, identify private deliverables and unresolved evidence, then stop for ChatGPT audit.

## Active authority — CG Arrow 014

The user has authorized the first **gated pristine-year reveal** for September 2024 through August 2025, but only after the Historical Data Certification Gate closes.

Frozen period:
- August 2024 warmup only;
- September 2024–August 2025 inclusive = twelve OOS signal months;
- August 2025 is the final OOS signal month;
- September 2025 may support late-August H8/H9/H10 lifecycle/runoff only;
- account starts at $100,000 before the first September 2024 cohort.

The latest public Arrow 013 state is still intermediate. Arrow 014 must verify local acquisition completion, close corporate-action/security-identity and selected-corridor lifecycle certification, audit August 2025 prior-use status, and commit `HOLDOUT_DATA_CERTIFIED_FOR_PRISTINE_REVEAL` before performance.

## Historical Data Certification Gate

No strategy/model/horizon/account scoring may occur on uncertified data.

Required sequence:
**retrieval/immutable landing → normalization → validation → certification → frozen reveal**.

Certification covers every observation capable of changing eligibility, selection, ranking, sizing, entry, exit, valuation, action units or risk.

At minimum:
- authoritative NYSE calendar, holidays, early closes, New York timezone/DST;
- timestamp uniqueness/order and session-window/minute-count logic;
- exact missing/stale/source-state inventory;
- OHLC/volume sanity;
- full point-in-time universe/ranking-field completeness;
- historically supportable security identity/type and exclusions;
- documented corporate actions/identity changes with precise price/share-volume/held-share normalization;
- split-consistent R5 volume/momentum corridors;
- causal pre-order entry observations;
- H8/H9/H10 lifecycle/exit observations;
- distinction among no trade, missing data, last-sale-ineligible states, vendor failure and halt/suspension;
- exact exception inventory and SHA-256 provenance.

Do not infer action factors from jumps, backfill current identity as historical fact, turn missing values into zero/favorable states, use stale prices as verified executions, interpret vendor failure as a market event, or substitute another rank because evidence is missing.

A strategy membership may be generated before P&L solely to identify the exact corridors requiring documented action/identity closure. If any material corridor remains unresolved, scoring stays locked.

## Pristine reveal governance

Create and push the reveal freeze before any outcome calculation. After the first outcome exists, no new horizon, multiplier, filter, model, threshold, substitution rule or cost/account definition may be added to this holdout.

Frozen principal matrix:
- R4 fixed-dollar H8/H9/H10;
- R5 fixed-dollar H8/H9/H10;
- R5 equity-scaled H8/H9/H10;
- C1 Rank-One 1.50x equity-scaled H8/H9/H10;
- Equal-Dollar H10 fixed control;
- C2 and C3 H10 fixed/equity falsification controls.

Principal quantity convention: causal pre-order. Equity-scaled books use each book's own causal signal-close marked-equity path. C1 uses exactly the Arrow 012 1.50x cohort-budget-neutral allocation rule. C2/C3 use their exact frozen Arrow 012 formulas. No parameter sweep.

## Required reporting

Every principal complete account publishes:
- full account economics/risk/exposure/reconciliation;
- every calendar month Sep 2024–Aug 2025 under `cg_lab_monthly_account_reporting_v1`;
- every weekly cohort's performance on the specified basis;
- H8→H9→H10 attribution;
- rank-one versus ranks-2–8 attribution;
- major drawdown episodes;
- exact-cell historical versus pristine comparison where available;
- frozen Arrow-011 mechanism confirmation panel.

Monthly P&L comes from the daily marked account, chains exactly, and reconciles to period-end marked P&L rather than eventual post-cutoff exits. Runoff/open obligations remain separately named.

## Scientific practice

- Preserve all Arrow 001–013 artifacts unchanged.
- Positive results never substitute for verification.
- Data certification and research contamination are separate locks.
- A component-level improvement does not count until the complete chronological engine is rebuilt.
- Interactions/failures remain evidence and are not hidden.
- Historical reference calculations after freeze cannot change the OOS rule.
- No post-reveal optimization may use Sep 2024–Aug 2025.
- Associations remain associations unless causality is separately established.

## Authentication / source policy

Use the official installed ThetaData SDK and the existing authorized Stocks Professional entitlement only where additional verification/retrieval is needed. No Theta Terminal, subscription/account changes, paid upgrades, options or sub-minute program. Total Theta concurrency may not exceed eight or any lower observed vendor/session limit. Preserve the Arrow 013 fail-closed session-expiry behavior and supervised resume logic.

Credentials remain in ignored `.env`; never print/log/serialize/stage/commit them. Silent empty-success responses are failures.

## Calendar policy

Nominal Wednesday signal; if closed, roll backward to the most recent exchange session. Entry is the first exchange session after signal. H8/H9/H10 count exchange sessions after actual fill. Preserve actual early closes and New York DST.

## Public/private safety

Never access `C:\Users\james\Money`. Work only in `C:\Users\james\money_chatgpt`. The GitHub repository is public. Raw vendor data, detailed selected-name/trade/account rows, credentials and sensitive evidence stay ignored/private. Public outputs carry reviewed aggregates, cohort economics without sensitive symbol detail where required, schemas, hashes and provenance. Inspect staged paths and contents before every push.

## Cost/naming conventions

Use human-readable names first. Headline economics use the inherited modeled commission/spread convention. Do not add generic 10%/30% borrow haircuts to normal tables; use the standing concise excluded broker-specific cost/availability footnote.

If certification cannot close, stop with exact blockers and no performance. If it closes, complete the whole predeclared one-batch reveal, push reviewed outputs, verify remote equality and stop for audit.