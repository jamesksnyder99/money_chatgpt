# CG Arrow 014 — Repair Addendum 1: certification closure, no performance

Authorized by ChatGPT as research director following the user's request for an audit with performance withheld whenever material defects call the run into question.

Executor: **Opus in Claude Code**. Repository: `jamesksnyder99/money_chatgpt`. Local root: `C:\Users\james\money_chatgpt`.

Read `reports/cg_arrow014_chatgpt_audit.md` first. It audits snapshot `297663414500e169cbd4261a11d6a6c7f3509125` and assigns `A14_AUDIT_NOT_CLEARED — PERFORMANCE_WITHHELD`.

**This addendum controls the present execution phase. It supersedes every older instruction that would automatically proceed from a generated gate to a reveal. Repair and verify the certification layer, commit/push the no-performance evidence package, and STOP for ChatGPT audit. Do not run the reveal again during this assignment.**

This is a repair to the existing Arrow 014, not authorization for Arrow 015, a new research program, or post-hoc optimization. The frozen strategy definitions remain unchanged.

## 1. Preservation, restart safety and spoiler protection

1. Check root, remote, working tree and processes. Preserve legitimate uncommitted work; fast-forward pull only when safe. Never reset, clean or overwrite raw data. Do not access `C:\Users\james\Money` or link to it.
2. Read `AGENTS.md`, `CLAUDE.md`, `docs/CHATGPT_LAB_PROTOCOL.md`, `docs/SUCCESS.md`, the original A14 arrow, both original addenda, the immutable reveal freeze, the submitted certification/handoff, and the ChatGPT audit.
3. Add a concise current repair/stop notice to `CLAUDE.md` without erasing historical authority or evidence. Preserve the original LOCK 1 and every submitted LOCK 2/reveal version. Mark submitted performance artifacts **not audit-cleared** through an append-only status/withdrawal manifest; do not delete or rewrite them into new results.
4. Version repair outputs and derived caches separately, using `repair01` or an equally explicit suffix. Bind the original snapshot and original freeze hash. Inventory existing private outputs by path/hash rather than copying their performance into the repair report.
5. Do not calculate, display, summarize, quote or hint at holdout strategy P&L, returns, win rates, drawdowns, ending balances, model rankings, month/cohort outcomes, rank-one outcomes or mechanism outcomes. Existing output values must not guide repair choices. Historical **pre-entry predictors**, including ranking returns and sizing features, may be reconstructed solely for certification.
6. No new model, threshold, hold length, allocation, universe band, substitution rule, execution-time convention, account rule or generic borrowing-cost deduction. The four parked pursuits remain parked.

## 2. Required repair lanes

### Lane A — Certify the full frozen matrix's dependencies

Replace the top-eight-only materiality test with a complete dependency ledger keyed by cohort, security identity, role, session and scored-cell dependency.

Cover:
- the complete point-in-time rule field capable of changing a selected or control rank, under the actual frozen prior-close and dollar-volume predicates;
- ordered ranks 1–20 and all information used to decide Off-High/Combined substitutions;
- the actual frozen lineups for every base and substitution book, generated without calculating subsequent outcomes;
- the full 20-prior-session volume baseline plus signal session, three-session momentum inputs and all other frozen substitution/diagnostic features;
- strictly pre-fill sizing observations, entry execution, every required daily mark, scheduled H8/H9/H10 exits and any documented delayed lifecycle/runoff dependency;
- action, identity and economic-claim continuity across those windows.

An unresolved candidate cannot disappear merely because an incomplete input kept it out of a shortlist. Establish why it cannot change a frozen scored cell or resolve it. A genuine insufficient listing-history exclusion must be proved and must match the existing rule, not inferred from an absent local file.

Fixed-point comparison must include the ordered candidate/control state that can change any frozen lineup, the selected lineups, feature/input fingerprints and the action/identity versions. An unchanged top-eight hash alone is insufficient. Gate counts must cover the union, with reconciled totals by role/cell and no orphan dependency.

### Lane B — Resolve missing feature evidence without inventing a sizing rule

Apply original A14 section 3.5: unknown inputs do not become zero or an unpenalized/FULL multiplier. The existence of an older Python default does not override this gate requirement.

Trace the reported missing/incomplete feature cases and discover any additional ones in the newly complete dependency set. For each, record expected sessions, observed source states, exact available inputs, adjusted units, resulting feature availability and evidence.

A documented no-trade session's share volume may be zero only when the source semantics establish that fact. That is different from assigning zero to an unavailable price, an absent response, positive-volume last-sale-ineligible prints, or an undefined return. Do not substitute EOD or carried prices into a frozen minute-based feature silently, compress missing sessions out of a fixed-session lookback, or fabricate a previous close.

Where valid source data restore a well-defined frozen feature, repair the derived measurement. Where the feature is genuinely undefined under the contract, keep an explicit blocker. Do not decide to skip, substitute, make FULL, HALF or QUARTER, or select a different window on the basis of subsequent performance. Document the unresolved semantic/policy question for ChatGPT instead. Do not change historical result files to conceal the discrepancy.

### Lane C — Separate source availability, tradability and economic lifecycle

Carry distinct, testable states for absent/unreadable input, vendor/authentication failure, zero-trade/zero-volume session, positive-volume prints with no usable last-sale price, usable price, documented halt/suspension, identity transition, and other documented lifecycle/settlement events.

`partition_resolved` plus `raw_rows > 0` proves neither a halt nor a continuously outstanding economic claim. Inspect the raw state, compatible EOD references and documented security events as necessary; retain discrepancies rather than treating all unpriced records as equivalent.

For each unusual entry, exit or mark, reconcile:
- what the frozen contract requires;
- the actual engine branch/status;
- exact observed timestamps and sources;
- documented event evidence, effective dates and identity continuity;
- whether an order never existed, remains unfilled, was carried under an authorized rule, executed later, or represents an outstanding position/settlement obligation.

In particular, correct the report/code mismatch: current entry logic records a missing entry rather than automatically carrying it; carried exits require the engine's documented-event predicate. Do not resolve the mismatch by silently introducing delayed entries or assuming a halt. Trace actual behavior and apply only a documented, authorized convention. If the convention is genuinely underdetermined, stop with the issue rather than choose an advantageous treatment.

No indefinitely stale liability may be certified merely because no usable price appears. Establish the appropriate documented economic status. Retain any unresolved position, mark or settlement as blocking where it can affect the matrix.

### Lane D — Prove causal pre-order sizing, especially on sparse sessions

Require a valid pre-order observation whose information is available strictly before the modeled fill. Checking non-null fields is insufficient. Account for minute-bar start versus bar-completion semantics and actual early closes.

The audited `summarize` implementation selects `preorder` from bars before the scheduled final minute. When execution falls back to an earlier final available print, that same print can be both sizing reference and execution. Measure every affected occurrence privately and fail the gate on the causal violation.

Reconcile the existing written execution convention with strictly prior information. Make no new favorable stale/backdated execution rule. If the original convention cannot be realized causally for a required row, record a blocker and the exact conflict for ChatGPT. Do not silently turn it into a zero-share/no-entry ticket. Provide causal tests for all ordinary, sparse and early-close branches.

### Lane E — Dated corporate actions, identity proof, cache correctness and provenance

For every material action, record the primary-source ratio, explicit effective trading date, security identity and source/publication context. Where a parser failed to read a date, inspect the actual filing or authoritative exchange/issuer notice and record that date; a uniquely fitting tape jump is corroboration, not dated authority. Keep unresolved/ambiguous dates blocking. Do not infer the absence of an action from a search that did not reach the correct issuer or relevant documents.

Verify coverage through the complete feature window and actual lifecycle, and reconcile current versus stale census-stage counts. A current ticker-to-CIK lookup alone must not establish historical security continuity. Bind any accepted negative-search evidence to its actual issuer, dates, document set and retrieval success.

Invalidate only affected derived caches under a new dependency fingerprint. The inspected summary-cache key uses file size/mtime and a version string; prove that calendar changes, selected source paths/identities, action tables and summary-code changes cannot return an old incompatible record. Include code/source/calendar/identity dependencies or a justified equivalent. Do not overwrite immutable raw partitions.

Reconstruct the actual chronology: original LOCK 1, original gate and outcome exposure, withdrawal, repairs, replacement gate and replacement run. Preserve the earlier exposure in an append-only ledger. Do not write `no_outcomes_calculated=True` for the whole corridor after a prior reveal. A later replay can be a corrected evaluation of originally predeclared rules; it cannot become a first-ever untouched reveal again.

Make the gate executable: require exact freeze/formula/cell/schema equality, all expected artifacts present, approved technical-change hashes, matching input/dependency hashes and row-level evidence coverage. Descriptive `rule`/`basis` keys are not proof. No permissive zero/True default may turn a missing required artifact into a pass.

## 3. Regression tests and independent checks

Run the relevant existing suite and add targeted failing-before/passing-after tests. Use synthetic fixtures and no holdout performance outputs. At minimum test:

- clean top eight plus an unresolved replacement/control: gate fails;
- changing ranks 9–20 with unchanged top eight: fixed point fails;
- a potentially material unresolved competitor outside a selected shortlist: cannot be silently omitted;
- unavailable volume/return and incomplete required feature sessions: cannot clear via favorable sizing defaults;
- no-trade zero volume versus positive-volume unpriced prints versus missing/vendor-failed input: distinct states;
- absent/equal-time/future pre-order observation: gate fails, including the last-print fallback and early closes;
- undocumented missing exit or economically ambiguous cessation: not certified as a documented halt;
- actual entry/exit order behavior disagrees with its claimed treatment: gate fails;
- ambiguous action date or historical identity: gate fails;
- required artifact absent, freeze changed, unapproved code drift, evidence hash mismatch, or stale cache: gate fails;
- a certificate carrying only explanatory strings cannot clear unresolved rows.

Where practical use an independent reviewer/worker to attack the dependency and gate checks without consulting strategy performance. Test counts alone do not certify the underlying observations.

A shared-engine repair may affect old-year computations. Preserve old artifacts and report the affected scope and reproduction hashes. Do not silently redefine prior-year comparisons or claim their values remain unchanged without evidence. Any later corrected historical comparison requires explicit versioning; no historical strategy retuning.

## 4. Work allocation and retrieval constraints

Reuse valid acquisition and evidence. This is not an instruction to download the entire minute layer again. Parallelize independent source investigations and tests within the host's capacity, with one writer per partition and no competing oversubscribed supervisors. Total Theta concurrency stays at eight or a lower vendor/session limit across the process tree.

Use the official installed SDK and existing Stocks Professional entitlement only. Preserve fail-closed authentication/session-expiry handling, ignored credentials, immutable raw landing, versioned repairs and public/private separation. No account/subscription change, paid expansion, options, streaming, sub-minute program or Theta Terminal. Report work stages, bottlenecks and unresolved counts without performance. Quality and evidence closure govern the stop; do not waive blockers to meet a clock target or keep working merely to consume time.

## 5. Deliverables and mandatory stop

Public-safe, no-performance artifacts:
- `reports/cg_arrow014_repair01_report.md`: each audit ID, exact cause, repair, source/code evidence, tests, unresolved items and shared-engine impact;
- `reports/cg_arrow014_repair01_manifest.json`: original/final input and code hashes, complete dependency counts, explicit tested gate conditions, exact exception totals and private evidence paths/digests;
- `reports/cg_arrow014_exposure_and_withdrawal_ledger.json`: accurate append-only provenance without performance values;
- executable tests and a reproducible repair command log.

Private/ignored evidence must include full per-dependency, feature, action/identity, source-state, causal-preorder and lifecycle ledgers. Stage compact audit-relevant evidence individually under `handoff/outgoing/cg_arrow014_repair01/`, not only inside a ZIP. Public reports may give technical counts and redacted examples but must not expose detailed selected names, raw licensed records, credentials or performance.

Final repair status is either:
- `A14_REPAIR01_BLOCKED` with exact unresolved dependencies; or
- `A14_REPAIR01_READY_FOR_CHATGPT_AUDIT`, supported by complete machine checks.

Neither status authorizes performance. A generated legacy passing status must not bypass this hold. Commit/push reviewed repair artifacts, verify remote equality, identify private evidence locations and **STOP for ChatGPT audit before another strategy replay**. The subsequent complete frozen-matrix rerun and all reporting will be separately cleared only after this repair audit passes.
