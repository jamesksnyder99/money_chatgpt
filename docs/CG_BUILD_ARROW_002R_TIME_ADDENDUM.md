# CG Arrow 002R — time-budget addendum

This file **overrides only the wall-clock budget section** of `docs/CG_BUILD_ARROW_002R.md`. All scientific governance, IS/OOS protection, hypotheses, reporting requirements, and stopping rules in Arrow 002 / 002R remain unchanged.

## Director ruling

The blocked Arrow 002 attempt performed no research hypothesis scoring, no finalist selection, and no OOS scoring. Treat that event as infrastructure overhead, not as consumption of the recursive research allowance.

CG Arrow 002R therefore receives a **fresh 180-minute soft wall-clock allowance**, beginning after `git pull --ff-only` and the start banner.

The purpose of the extra time is not to increase the number of half-finished branches. It is to ensure the required directed work, meaningful Astra-originated discovery, finalist freeze, one-shot OOS confirmation, reporting, tests, commit, and push all close inside the same research session.

## Planning allocation

Use approximately:

- **0–10 min:** resume/preflight; build/reuse the IS-only scoring harness/cache; do not reopen the corporate-action gate as a blocker.
- **10–100 min:** ChatGPT-directed research corridors. Complete D1–D5 core tests, recursively refine only completed promising branches, and keep all work IS-only.
- **100–160 min:** protected Astra autonomous shop time. Originate genuinely new, causal, interpretable IS-only hypotheses; complete every hypothesis opened; challenge the current frontier rather than merely cloning directed parameter variants.
- **160–170 min:** write the finalist freeze manifest, then perform the **single** OOS scoring pass for the frozen parent and frozen finalists.
- **170–180 min:** continuous 12-month investor views, borrow-cost sensitivity, final research ledger/report, focused + full tests, public-safety review, commit, and push.

This gives approximately **90 minutes directed / 60 minutes Astra-originated discovery** (60/40 of the 150-minute discovery block), with ~30 minutes reserved for infrastructure and scientific closure.

The split is a planning allocation, not a rigid quota. If a strong Astra-originated branch warrants additional time, Astra may exceed 60 minutes, provided all mandatory directed core tests have completed and the OOS/reporting closure budget is protected.

## Completeness rule

Every hypothesis actually opened in this run must finish with a scored IS result or a specific genuine branch-level blocker before the arrow ends.

Do not leave a pending queue. If time becomes scarce, stop opening new hypotheses and **reduce breadth, not completeness**.

Do not stop early because one improvement appears. Use remaining IS time to challenge it, falsify it, search for distinct alternatives, and test narrowly motivated neighboring specifications when useful.

## Overrun

A modest overrun of up to approximately **15 minutes** is permitted only to finish:

- a hypothesis already executing before minute 180;
- the already-frozen one-shot OOS pass;
- required reporting/tests;
- commit/push.

Do not open a new research branch after minute 180.

## OOS firewall unchanged

The additional time does **not** authorize any extra OOS visibility. Even-month signal cohorts remain protected until finalists are frozen. There is still exactly one OOS reveal and no post-OOS tuning within this arrow.
