# money_chatgpt economic and evaluation policy

## Active mandate — CG Arrow 014, September 14, 2026

**Opus in Claude Code** is assigned the gated pristine-year reveal. Exact scope: `docs/CG_BUILD_ARROW_014.md`.

No performance may be calculated until the holdout receives the exact certification status:

`HOLDOUT_DATA_CERTIFIED_FOR_PRISTINE_REVEAL`

The latest public Arrow 013 record is intermediate; Arrow 014 must verify the completed local ingest and close all material identity/corporate-action/lifecycle issues before unlocking the reveal.

## Frozen pristine OOS period

- August 2024 = warmup only.
- September 2024 through August 2025 inclusive = twelve OOS signal months.
- August 2025 = final OOS signal month.
- September 2025 may support late-August H8/H9/H10 lifecycle/runoff only.
- Each complete holdout account starts from $100,000 before the first September 2024 cohort.

August 2025 must also pass a prior-use audit proving it was not previously strategy-scored as a signal month before the 12-month block is called pristine.

## Certification success definition

Before scoring, Arrow 014 must prove:
1. Arrow 013 acquisition stages are genuinely complete under their declared inventory contract;
2. vendor/session errors cannot silently remove required observations;
3. calendar/timestamps/DST/early-close handling is correct;
4. full point-in-time universe/ranking-field coverage is sufficient for frozen membership;
5. security identity/type is historically supportable;
6. every material corporate action/identity issue capable of changing membership/features/holding units is documented and normalized precisely;
7. R5 momentum and 20-session volume corridors are split-consistent and complete;
8. causal pre-order entry observations are certified;
9. H8/H9/H10 lifecycle/exit observations and any event/open status are certified;
10. unresolved material exceptions affecting a scored cell equal zero;
11. no strategy performance was calculated before gate opening.

Unknown data do not become zero/favorable, vendor failure does not become a halt, stale price does not become a verified execution, and missing evidence does not trigger next-rank substitution.

## Frozen reveal matrix

Primary fixed-dollar strategy replication:
- Volume-Sized Short (R4): H8, H9, H10.
- Momentum+Volume-Sized Short (R5): H8, H9, H10.
- Equal-Dollar Short H10 as a diagnostic baseline.

Primary account-engine replication:
- R5 with Arrow-010 equity scaling: H8, H9, H10.
- C1 Rank-One 1.50x cohort-budget-neutral reallocation with equity scaling: H8, H9, H10.

Secondary falsification controls:
- C2 Off-High Substitution H10 fixed-dollar and equity-scaled.
- C3 Combined H10 fixed-dollar and equity-scaled.

Principal quantity convention is CAUSAL_PREORDER_QTY. No multiplier, horizon, filter, substitution threshold, model or account rule may be added after the reveal freeze is committed.

## Success language

The pristine year is a replication test, not a new training set.

For each model/horizon publish complete economics rather than only ending equity: marked/eventual P&L, return, PF, win/loss structure, DD $/%, worst day, underwater time, gross exposure/gross-to-equity, turnover/concentration, monthly shape, runoff/open obligations and exact account reconciliation.

For C1 use one of:
- `C1 PRISTINE REPLICATION — IMPROVES COMPLETE ENGINE WITH ACCEPTABLE/DECLARED RISK TRADEOFF`;
- `C1 PRISTINE REPLICATION — IMPROVES PROFIT BUT MATERIAL RISK/CONCENTRATION COST`;
- `C1 DOES NOT REPLICATE`;
- `C1 INCONCLUSIVE — exact reason`.

No result on this year authorizes immediate post-hoc parameter tuning on the same period.

## Mandatory reporting

`cg_lab_monthly_account_reporting_v1` applies to every principal complete account: all 12 individual months, monthly P&L, monthly return on prior month-end equity, month-end equity, exact cent chaining, summary counts and cutoff/runoff treatment.

Publish every weekly cohort in the required public-safe matrices, including R4/R5 H8/H9/H10 fixed-dollar and R5/C1 equity H8/H9/H10, with cohort breadth summaries. For equity-responsive cohorts expose signal-close reference equity/scale context so later larger-dollar cohorts are not mistaken for intrinsically better signals.

Also publish:
- rank-one versus ranks-2–8 confirmation;
- frozen Arrow-011 mechanism checks with no refitting;
- H8→H9 and H9→H10 attribution;
- major drawdown episodes;
- exact-cell historical versus pristine comparisons where available;
- C2/C3 falsification outcomes.

## Research contamination rule

After the first pristine outcome is visible, do not add H7/H6, a different rank-one multiplier, new filter, new R5 rule, new substitution threshold/rank depth, new cost convention or new model. Ideas inspired by the reveal are queued for a future development block only.

Historical matching references may be calculated after the Arrow 014 freeze under exactly the same rules, but cannot alter the OOS definitions.

## Data/source policy

Use the Historical Data Certification Gate and official installed ThetaData SDK under the existing Stocks Professional entitlement where remaining verification/retrieval is required. No new paid data, subscription/account changes, options, sub-minute program or Theta Terminal. Credentials remain ignored and secret. Preserve immutable raw data and versioned repairs.

## Calendar/action policy

Nominal Wednesday signal; roll backward to the most recent exchange session if closed. Entry is first valid exchange session after signal. Hn is n exchange sessions after fill. Preserve actual early closes/New York DST. Corporate actions use documented dated evidence and exact unit normalization; price-jump screens are triage only.

## Cost convention

Headline results use the inherited commission/spread model. Do not carry generic 10%/30% borrowing deductions into normal tables. Use the standing concise footnote for excluded broker-specific locate/HTB charges, dividends, financing, forced-close effects, taxes and other account-specific items.

## Delivery

Preserve all Arrow 001–013 artifacts. Public GitHub gets reviewed safe aggregates, schemas, hashes, code/tests and provenance. Raw vendor data, selected-symbol/trade detail, primary-source evidence and daily account ledgers remain private/ignored as appropriate.

If certification fails, stop with exact blockers and no strategy performance. If certification passes, finish the entire frozen one-batch reveal before stopping for ChatGPT audit.