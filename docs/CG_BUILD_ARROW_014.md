# CG Build Arrow 014 — Gated Pristine-Year Reveal Matrix

Executor: **Opus in Claude Code**. Research director/auditor: ChatGPT.

Authorized September 14, 2026 after the Arrow 013 ingest. This arrow performs the first strategy reveal on the previously uninspected September 2024–August 2025 holdout, but ONLY after a hard certification gate closes every material data issue. It is a one-batch reveal, not an optimization cycle.

## 1. Frozen period and purpose

The pristine OOS signal period is **September 2024 through August 2025 inclusive — 12 signal months**.

- August 2024 is warmup only and owns no signal cohort.
- August 2025 is the twelfth/final OOS signal month.
- September 2025 may be used only as lifecycle/runoff support for late-August 2025 positions.
- The pristine account starts fresh at **$100,000** immediately before the first September 2024 cohort.
- The expected weekly cohort count is 52 under the frozen Wednesday/holiday-rollback calendar. Prove the actual frozen cohort list from the calendar before scoring; do not force 52 if a genuine exchange-calendar fact says otherwise.

Mission:

1. finish/verify holdout data certification fail-closed;
2. commit one immutable reveal manifest before any performance is calculated;
3. score the entire predeclared model/horizon matrix in one batch;
4. publish full account economics, all 12 months, and every weekly cohort;
5. compare the pristine year with the historical year where an exact matching cell exists;
6. perform the pre-registered mechanism checks without tuning anything from the reveal.

No model, threshold, hold, sizing multiplier, filter, replacement rule or reporting definition may be changed after the reveal manifest is committed.

---

# 2. Workspace, provenance, and preservation

Work only in `C:\Users\james\money_chatgpt`, remote `jamesksnyder99/money_chatgpt`. Never access `C:\Users\james\Money`.

Pull `main` with `git pull --ff-only origin main` without overwriting legitimate local work. Read `AGENTS.md`, `CLAUDE.md`, `docs/CHATGPT_LAB_PROTOCOL.md`, `docs/SUCCESS.md`, this arrow, Arrow 013 certification/acquisition reports/manifests, and Arrows 007–012 needed for frozen strategy definitions and historical comparison values.

Preserve every Arrow 001–013 artifact. Do not rewrite old results to match new findings.

The latest committed Arrow 013 state when this arrow was authored is not yet sufficient by itself to unlock scoring: the public record still shows an intermediate minute-layer inventory and unresolved dated corporate-action/identity certification. The user reports the ingest has completed locally; Arrow 014 must verify that completion from local immutable data and close the certification gate before any outcome scoring.

---

# 3. PHASE 0 — HARD DATA CERTIFICATION GATE BEFORE PERFORMANCE

**No trade P&L, hit rate, model comparison, ending equity, monthly strategy return, rank-one outcome, H8/H9/H10 comparison, or selected-name outcome may be calculated until this gate passes.**

Strategy membership may be mechanically generated after the reveal definitions are frozen if needed solely to identify the exact corridors requiring primary-source action/identity certification, but outcome scoring remains locked until those corridors are certified.

## 3.1 Arrow 013 acquisition completion

Verify the final local Arrow 013 stage state rather than relying on the intermediate GitHub count.

At minimum:

- authenticated vendor/session provenance remains valid and no credential is exposed;
- all intended Arrow 013 acquisition stages report complete under their declared inventory contract;
- no outstanding `UNAUTHENTICATED`, session-expiry, vendor-error, or silent-empty condition can remove an observation that could affect the reveal;
- every acquired immutable partition has stable size/hash/provenance and is readable;
- the supervisor did not reinterpret a genuine no-data response as a successful partition;
- EOD/full-universe and one-minute layers have exact completeness inventories by session/source state;
- August 2025 overlap authentication remains clean or any required re-retrieval is separately versioned and reconciled;
- September 2025 lifecycle-tail support is complete for every late-August selected corridor eventually required by H8/H9/H10.

Do not hide benign vendor conventions. Preserve distinctions among no-trade minutes, missing bars, last-sale-ineligible consolidated-volume states, vendor failures, halts/suspensions and valid traded observations.

## 3.2 Calendar and timestamp certification

Re-assert the frozen 2024–2025 NYSE calendar against the independent reference used by Arrow 013:

- closures/holidays;
- early closes;
- New York DST;
- expected RTH schedule/minute counts;
- timestamp unit alignment;
- unique/ordered in-window bars.

The strategy calendar remains:

- nominal Wednesday signal;
- if Wednesday is closed, roll backward to the most recent exchange session;
- entry is first valid exchange session after signal;
- H8/H9/H10 exit is 8/9/10 exchange sessions after actual fill.

## 3.3 Point-in-time universe/ranking-field certification

The complete field capable of affecting top-eight membership must be certified before scoring. Confirm:

- point-in-time common-stock eligibility under the frozen rule;
- $10–$80 prior-close eligibility;
- prior-day dollar volume >= $10M;
- exclusion/type handling is historically supportable; where the roster itself is common-stock-only and the ETP denylist is a no-op, state that accurately rather than pretending it actively filtered;
- complete 15-session ranking history for every candidate that can affect membership;
- missing data cannot silently remove a competitor;
- security identity is point-in-time rather than present-day backfill.

## 3.4 Corporate-action and security-identity closure

Arrow 013 correctly refused to certify this from price jumps alone. Close it now before P&L.

Sequence:

1. Freeze and commit the reveal definitions in section 4 before outcome scoring.
2. Mechanically build the candidate/ranking membership needed by the frozen cells **without calculating outcomes**.
3. Establish an event/identity census across every ranking/feature/holding corridor capable of altering:
   - top-eight membership;
   - R4/R5 sizing features;
   - rank-one identity;
   - C2/C3 ranks 9–20 controls;
   - H8/H9/H10 quantity/value units.
4. For every material action/identity issue, resolve from documented dated evidence. Price discontinuity is triage, never the proof.
5. Normalize price/share-volume/held-share units precisely under the established action convention, then rerank mechanically if any action/identity correction changes input units.
6. Iterate until newly selected names introduce no unresolved material event and no unresolved candidate can change the required memberships.

No manual rank substitution because evidence is missing. If a materially required security remains unresolved, certification fails and scoring stays locked.

## 3.5 R5 volume/momentum feature certification

For every R5-selected or C2/C3 control name, certify the exact feature window in split-consistent units:

- three-session return;
- signal-session share volume;
- arithmetic mean of previous 20 RTH sessions;
- exact `> 0` momentum and `> 1` volume operators;
- missingness remains missing, never favorable zero/FULL state.

## 3.6 Causal entry and lifecycle certification

For every name used by any scored cell, prove:

- causal pre-order observation is complete and strictly pre-fill;
- the inherited execution reference exists under the frozen convention;
- H8/H9/H10 lifecycle observation exists or has a documented event/open status;
- no stale-price rescue, next-name substitution or future-based omission;
- any documented halt/suspension/open obligation remains explicit.

## 3.7 August 2025 pristine-status audit

Before calling the period pristine, search prior public and private research artifacts/manifests for strategy-scored **signal cohorts owned by August 2025**.

Warmup/lookback use is allowed. A strategy outcome, selected cohort, P&L, hit rate, horizon score or account result owned by an August 2025 signal cohort would mean August 2025 is not pristine and must be disclosed before scoring.

Require a machine-readable audit result with evidence paths searched and status:

- `AUGUST_2025_NOT_PREVIOUSLY_SCORED_AS_SIGNAL_MONTH`, or
- `AUGUST_2025_PREVIOUSLY_EXPOSED — exact evidence`.

If the latter occurs, stop and report rather than silently calling the 12 months pristine.

## 3.8 Gate status

Write and commit a public-safe certification report/manifest before any outcome scoring. Scoring may proceed only with exactly:

`HOLDOUT_DATA_CERTIFIED_FOR_PRISTINE_REVEAL`

Anything else stops the arrow before performance.

Required certification report must state counts, hashes, unresolved exceptions = 0 for every observation capable of changing the frozen scored cells, and that **NO STRATEGY PERFORMANCE HAD BEEN CALCULATED BEFORE THE GATE OPENED**.

---

# 4. PRE-REVEAL FREEZE — COMMIT BEFORE PERFORMANCE

Create and push `reports/cg_arrow014_reveal_freeze.json` before calculating any strategy outcome. It must contain the exact cohort calendar, data/certification hashes, code hashes, formulas, models, horizons, account conventions, metrics, hypothesis checks and output schemas below.

This freeze is immutable after reveal begins.

## 4.1 Principal quantity/account convention

Principal simulation convention: **CAUSAL_PREORDER_QTY**.

Observed historical bars are simulation references, not guaranteed broker fills.

Headline economics use the inherited commission/spread model. Do not introduce generic 10%/30% stock-loan haircuts into headline tables. Use the standing concise footnote that broker-specific locate/HTB charges, dividends, financing, forced-close effects, taxes and other account-specific execution items are excluded.

## 4.2 Core fixed-dollar horizon matrix — primary strategy replication

Score exactly these six cells on the pristine year:

1. Volume-Sized Short (R4) — H8 — fixed-dollar — causal pre-order quantities.
2. Volume-Sized Short (R4) — H9 — fixed-dollar — causal pre-order quantities.
3. Volume-Sized Short (R4) — H10 — fixed-dollar — causal pre-order quantities.
4. Momentum+Volume-Sized Short (R5) — H8 — fixed-dollar — causal pre-order quantities.
5. Momentum+Volume-Sized Short (R5) — H9 — fixed-dollar — causal pre-order quantities.
6. Momentum+Volume-Sized Short (R5) — H10 — fixed-dollar — causal pre-order quantities.

Also score **Equal-Dollar Short H10 fixed-dollar** as a diagnostic baseline/control, not as the principal race.

These fixed-dollar cells answer whether the Winner-Fade stock-selection/sizing edge and H8/H9/H10 economics replicate without compounding.

## 4.3 Equity/account-engine matrix

Score exactly:

7. R5 equity-scaled — H8.
8. R5 equity-scaled — H9.
9. R5 equity-scaled — H10.
10. Rank-One 1.50x reallocation (C1) equity-scaled — H8.
11. Rank-One 1.50x reallocation (C1) equity-scaled — H9.
12. Rank-One 1.50x reallocation (C1) equity-scaled — H10.

Equity scaling uses the frozen Arrow 010 rule:

- cohort reference = that account's marked equity at the signal-session close;
- scale = reference equity / 100,000;
- fixed base allocations are multiplied by that causal scale;
- integer shares derive from the same causal pre-order price convention.

C1 uses the exact Arrow 012 1.50x rule and no other multiplier:

- retain all eight original top-eight names;
- rank-one base target = 1.50 × its incumbent base allocation;
- fund the increase entirely by proportional reductions to ranks 2–8 so the cohort's base intended capital is unchanged;
- then apply that challenger's own causal equity scale.

Do not test 1.25x, 1.75x, 2.00x or any other multiplier.

H8/H9 versions are prospective frozen cross-products of already-established hold and sizing rules. They are not selected from this holdout.

## 4.4 Frozen falsification controls

At H10 only, carry the already-frozen Arrow 012 challengers as secondary controls:

13. C2 Off-High Substitution — fixed-dollar H10.
14. C2 Off-High Substitution — equity-scaled H10.
15. C3 Combined Rank-One Reallocation + Off-High Substitution — fixed-dollar H10.
16. C3 Combined — equity-scaled H10.

Use the exact Arrow 012 formulas and exact A11 off-high threshold. Do not retune the threshold/search depth or substitution budget rule on the pristine year.

These are falsification/replication controls, not candidates to optimize after seeing results.

---

# 5. ONE BATCH ONLY — NO MID-REVEAL TUNING

After certification and freeze are committed, calculate all predeclared cells in one controlled batch.

Forbidden after first outcome is visible:

- new horizon (H7, H6, etc.);
- new rank-one multiplier;
- new model or filter;
- altered R5 momentum/volume logic;
- new substitution threshold/rank depth;
- dropping a bad month/cohort;
- changing capital, cost or reporting definitions;
- re-labeling an observed winner as the planned model.

Any idea inspired by the pristine outcomes goes into a future development block. It is not tested on this holdout.

---

# 6. REQUIRED RESULT LAYER 1 — HEADLINE MATRIX

The first report table must be:

| Model | H8 marked P&L | H9 marked P&L | H10 marked P&L | best observed H (descriptive only) | max DD | PF | positive months | positive cohorts |
|---|---:|---:|---:|---|---:|---:|---:|---:|
| R4 fixed-dollar | | | | | | | | |
| R5 fixed-dollar | | | | | | | | |
| R5 equity-scaled | | | | | | | | |
| C1 rank-one 1.50x equity-scaled | | | | | | | | |

`best observed H` is descriptive. It does not authorize choosing or retuning a hold from this OOS block.

Immediately below, publish the fixed H10 controls: Equal-Dollar H10, C2 fixed/equity H10, C3 fixed/equity H10.

---

# 7. REQUIRED RESULT LAYER 2 — FULL ACCOUNT ECONOMICS

For every complete account cell publish:

- starting equity;
- ending marked equity at the last exchange close of August 2025 (expected 2025-08-29; verify from calendar);
- marked account P&L at that cutoff;
- eventual completed-trade P&L including properly named September runoff;
- return on starting equity;
- intended / filled / completed / open/event counts;
- wins/losses/flats, hit rate;
- average winner / average loser, payoff ratio;
- profit factor;
- maximum drawdown dollars and percent of its own running peak;
- drawdown peak/trough/recovery dates;
- worst day;
- time underwater;
- mean / p95 / peak gross exposure dollars;
- mean / p95 / peak gross divided by same-session marked equity;
- entry turnover/notional;
- largest single-name P&L share;
- rank-one entry-notional share and P&L share where applicable;
- positive/red/flat month count;
- sum of red months;
- worst/best/median month;
- runoff-trade count and post-cutoff incremental runoff P&L;
- stale gross/open documented obligations at boundary;
- exact daily cash-minus-short-liability account identity.

Do not compare strategies using only ending equity.

---

# 8. REQUIRED RESULT LAYER 3 — ALL 12 CALENDAR MONTHS

Apply `cg_lab_monthly_account_reporting_v1` to every principal account.

Publish September 2024 through August 2025 individually with:

- monthly marked-account P&L dollars;
- monthly return % on prior month-end equity;
- month-end marked equity;
- exact cent-level chaining;
- exact reconciliation to period-end marked account P&L;
- boundary/runoff treatment.

Provide a readable side-by-side principal table for at least:

- R4 H8/H9/H10 fixed-dollar;
- R5 H8/H9/H10 fixed-dollar;
- R5 equity H8/H9/H10;
- C1 equity H8/H9/H10.

If one table becomes too wide, split it into these four panels without omitting a month.

---

# 9. REQUIRED RESULT LAYER 4 — EVERY COHORT

Publish exactly one row for every pristine signal cohort in a public-safe cohort matrix and a private detailed audit.

## 9.1 Fixed-dollar core cohort matrix

Columns:

- cohort ID / nominal Wednesday / actual signal date / entry date;
- R4 H8 eventual cohort P&L;
- R4 H9 eventual cohort P&L;
- R4 H10 eventual cohort P&L;
- R5 H8 eventual cohort P&L;
- R5 H9 eventual cohort P&L;
- R5 H10 eventual cohort P&L;
- winning/losing/open position counts for the H10 principal lineage;
- total intended base cohort notional;
- rank-one sizing-neutral H10 return;
- rank-one fixed-dollar H10 P&L;
- ranks 2–8 fixed-dollar H10 P&L;
- best and worst position contribution (public report may omit symbol; private file keeps it).

The cohort P&L basis is eventual modeled P&L owned by original signal cohort. State it explicitly and reconcile row sums to eventual trade economics.

## 9.2 Equity/C1 cohort matrix

Columns:

- cohort ID / signal date;
- R5 equity H8/H9/H10 eventual cohort P&L;
- C1 equity H8/H9/H10 eventual cohort P&L;
- each account/horizon's causal signal-close reference equity and scale factor, or provide those in a normalized linked table if width becomes excessive;
- actual intended cohort notional after equity scaling;
- rank-one P&L and ranks 2–8 P&L;
- rank-one share of cohort P&L;
- completion/open status.

Do not compare later-dollar cohort sizes as if they were pure signal quality; always expose the reference-equity/scale-factor context.

## 9.3 Cohort breadth summaries

For every principal cell report:

- positive / negative / flat / partly open cohort count;
- median cohort P&L;
- best / worst cohort;
- top 1 / 3 / 5 cohorts' share of total positive/total net P&L, clearly named;
- bottom 1 / 3 / 5 contribution;
- longest run of positive and negative cohorts;
- number of distinct securities and repeated/overlapping episodes.

---

# 10. PRE-REGISTERED MECHANISM CONFIRMATION PANEL

Use only definitions already frozen in Arrow 011/012. Do not learn new thresholds from the pristine year.

At minimum test and report:

1. **Rank-one effect** — rank one versus ranks 2–8 on sizing-neutral H10 return: mean, median, hit rate, support by cohort, P&L contribution.
2. **Rank-one gap/extremeness** — exact A11 definition/threshold if thresholded; otherwise continuous frozen feature relationship.
3. **Off-high among ranks 2–8** — use the exact A11 frozen `close_vs_high20` cuts/median definition; no refit.
4. **Price-band/off-high state map** — exact A11 frozen bins, including counts.
5. **Wide-range/volatility descriptors** that A11 labeled recurring — same frozen feature definitions/cuts.
6. **Episodic-advance descriptors** — same frozen definitions/cuts.
7. **R5 three-session momentum sign** — test whether the IS separation that failed old internal confirmation appears in the pristine year; do not alter sizing from this result.
8. **Pre-order incremental-information checks** — same frozen descriptors; test rather than proclaim "no information" broadly.
9. **Repeat-selection episode effect** — same frozen definition.
10. **Holding-path anatomy** — rank one and ranks 2–8 at ages 1..10, close-only sizing-neutral paths, with H8/H9/H10 highlighted.

Classify each as:

- `PRISTINE_REPLICATION`;
- `SAME_DIRECTION_WEAKER`;
- `NOT_REPLICATED`;
- `INCONCLUSIVE_SPARSE`;
- `DATA_LIMIT`.

The pristine mechanism panel is evidence about existing hypotheses only. No new hypothesis is tested after reveal.

---

# 11. HORIZON ATTRIBUTION

For R4 and R5 fixed-dollar, and separately for R5 equity/C1 equity, explain H8→H9 and H9→H10:

- incremental eventual P&L;
- number of cohorts improving/deteriorating;
- number of individual positions improving/deteriorating;
- rank-one versus ranks 2–8 incremental contribution;
- effect on max drawdown/worst day/exposure using actual complete accounts rather than additive guesses.

Do not declare a new preferred production hold solely because one pristine year has the highest Hn. Report robustness across H8/H9/H10.

---

# 12. C1 VERSUS INCUMBENT — PRIMARY CHALLENGER TEST

At H10, and secondarily H8/H9, compare C1 to the corresponding R5 equity account.

C1 success/failure must include:

- full-account marked P&L difference;
- fixed capital/allocation logic confirmation (same base cohort capital);
- fixed-dollar diagnostic if needed to separate pure reallocation from compounding;
- max DD dollars/%;
- worst day;
- peak gross/equity;
- rank-one concentration;
- monthly differences;
- cohort breadth;
- whether rank-one sizing-neutral edge itself repeats.

C1 is not promoted merely for higher ending equity. State one of:

- `C1 PRISTINE REPLICATION — IMPROVES COMPLETE ENGINE WITH ACCEPTABLE/DECLARED RISK TRADEOFF`;
- `C1 PRISTINE REPLICATION — IMPROVES PROFIT BUT MATERIAL RISK/CONCENTRATION COST`;
- `C1 DOES NOT REPLICATE`;
- `C1 INCONCLUSIVE — exact reason`.

Do not invent a new multiplier after reveal.

---

# 13. C2/C3 FALSIFICATION CONTROLS

Use the exact Arrow 012 formulas at H10 only.

Report whether pristine results agree with the historical observations that:

- C2 off-high substitution under its full budget-normalized engine did not improve the historical incumbent;
- C3 improved historically versus C0 but was dominated by C1 due negative interaction.

No retuning, slot-inheritance variant, different search depth, different threshold or post-hoc repair is authorized in this arrow. Any such idea goes to a future development block.

---

# 14. HISTORICAL YEAR VERSUS PRISTINE YEAR

Publish a side-by-side comparison for exact-matching frozen cells where historical results exist.

At minimum:

- R4 H8/H9/H10 fixed-dollar;
- R5 H8/H9/H10 fixed-dollar;
- R5 equity H10;
- C1 equity H10;
- Equal-Dollar H10 control;
- C2/C3 H10 controls where exact historical cell exists.

For new prospective cross-product cells (for example R5 equity H8/H9 or C1 H8/H9 if no exact old-year result was previously frozen), do not fabricate a historical comparison. Either compute the old-year matching cell **after the Arrow 014 freeze under exactly the same frozen rule and without using it to change anything**, labeled `retrospective matching reference`, or leave historical comparison unavailable. If computed, no rule may change afterward.

Compare:

- total/marked P&L;
- return;
- PF;
- max DD;
- worst day;
- positive months/cohorts;
- rank-one contribution;
- exposure/concentration.

Do not average the two years into one annual expectation without also showing each year separately.

---

# 15. DRAWDOWN ANATOMY

For principal cells, publish the largest drawdown episodes, not just the maximum scalar:

- peak date;
- trough date;
- recovery date or unrecovered status;
- depth dollars and percent;
- duration;
- largest contributing signal cohorts;
- rank-one versus ranks 2–8 contribution when meaningful.

This is especially mandatory for C1 because its historical benefit came with greater concentration/worst-day risk.

---

# 16. AUDIT / INVARIANTS

At minimum test:

1. Phase-0 certification status equals `HOLDOUT_DATA_CERTIFIED_FOR_PRISTINE_REVEAL` before any outcome file timestamp/code path can exist.
2. Reveal freeze is committed before first outcome calculation.
3. Scored signal cohorts are exactly the frozen Sep 2024–Aug 2025 cohort list; August 2024 and September 2025 own zero signals.
4. August 2025 pristine-status audit passed before reveal.
5. R4/R5 top-eight membership is identical across H8/H9/H10 and sizing views unless a model definition itself says otherwise; horizons cannot alter selection.
6. C1 names are exactly the incumbent top eight; only allocation changes.
7. C1 base cohort capital equals incumbent base cohort capital to full precision before share flooring.
8. C2/C3 formulas/hashes equal the Arrow 012 frozen definitions.
9. Causal pre-order shares use no fill/future information.
10. Equity-scaled books use each book's own prior causal signal-close account equity, not another book's path.
11. Same-cell fixed-dollar trades differ across H8/H9/H10 only where the frozen horizon changes exit/lifecycle.
12. Corporate-action share/price/value identities reconcile for every affected selected trade.
13. Every monthly table chains exactly to its own cutoff marked account result.
14. Eventual P&L, cutoff marked P&L, runoff and open obligations are separate named quantities.
15. Every cohort matrix sums/reconciles to the corresponding eventual trade economics.
16. Daily cash minus short liability identities hold for every complete account.
17. Mechanism predictors use only frozen pre-entry feature definitions; no outcome leakage.
18. Historical reference cells cannot change reveal rules after calculation.
19. No new parameter/model/horizon appears after freeze.
20. Public/private separation passes content inspection; selected symbols/detailed vendor data remain private if current policy requires.

Run/extend the independent oracle so trade arithmetic, account marks, allocation/share quantities and cohort/month totals are independently recomputed from stored inputs rather than trusted from the scoring engine.

---

# 17. REQUIRED OUTPUTS

Public-safe, at minimum:

- `reports/cg_arrow014_certification_gate.md`
- `reports/cg_arrow014_reveal_freeze.json`
- `reports/cg_arrow014_headline_matrix.csv`
- `reports/cg_arrow014_account_summary.csv`
- `reports/cg_arrow014_monthly_account.csv`
- `reports/cg_arrow014_cohort_fixed_matrix.csv`
- `reports/cg_arrow014_cohort_equity_matrix.csv`
- `reports/cg_arrow014_cohort_breadth.csv`
- `reports/cg_arrow014_mechanism_confirmation.csv`
- `reports/cg_arrow014_horizon_attribution.csv`
- `reports/cg_arrow014_drawdown_episodes.csv`
- `reports/cg_arrow014_historical_vs_pristine.csv`
- `reports/cg_arrow014_reveal.md`
- `reports/cg_arrow014_manifest.json`
- `reports/cg_arrow014_commands.txt`
- necessary reviewed code/tests.

Private/git-ignored, individual/unzipped as needed:

- selected-trade audit for every scored cell;
- selected-name action/identity evidence ledger;
- rank 9–20 control candidate audit for C2/C3;
- detailed 52-cohort dossiers with symbols;
- daily account paths;
- allocation/share reconciliation files;
- event/open-obligation ledger;
- any primary-source evidence/caches not safe for public GitHub.

No giant ZIP is required.

---

# 18. REPORTING ORDER

The final user-facing report should be organized in this order:

1. Data certification/pristine-status gate.
2. Four-row headline H8/H9/H10 matrix.
3. C0 vs C1 H10 primary challenger result.
4. Full account economics.
5. All 12 months.
6. All 52 cohorts.
7. Rank-one/mechanism confirmation.
8. Horizon attribution.
9. Drawdown anatomy.
10. C2/C3 falsification controls.
11. Historical vs pristine year.
12. What replicated / failed / remains unresolved.

Use human-readable names first, legacy IDs second.

---

# 19. RUNTIME AND EXECUTION DISCIPLINE

This is a high-value reveal, not a speed contest. Predicted wall-clock depends primarily on final corporate-action/identity closure and any missing lifecycle retrieval, not on scoring.

Use up to eight Theta requests only where still required and within vendor/session constraints. Preserve the Arrow 013 supervised-session/fail-closed lessons. Parallelize independent evidence corridors where safe; selection/action normalization/rerank iterations and equity-compounding paths remain deterministic and appropriately serial.

If the data gate cannot close, stop with exact blockers and **no performance**. Do not relax certification standards to reach the reveal.

If the gate closes, finish the complete predeclared reveal matrix in the same arrow. Do not stop after seeing an exciting C1 or Hn result.

Commit/push reviewed public-safe outputs to `main`, verify remote equality, state private deliverable paths, and stop for ChatGPT audit. Do not begin a post-OOS optimization arrow.
