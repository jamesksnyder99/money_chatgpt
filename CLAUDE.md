# Claude Code — money_chatgpt

Read `AGENTS.md`, `docs/CHATGPT_LAB_PROTOCOL.md`, `docs/SUCCESS.md`, and the explicitly assigned CG arrow before acting.

**Current assignment: `docs/CG_BUILD_ARROW_011.md`. Executor: Fable in Claude Code.**

Winner-Fade Anatomy: combine Branch D (pre-entry signal anatomy and model/month differences) with the user's within-cohort winner/loser investigation. Principal subject: Momentum+Volume-Sized Short (R5), Corrected-Universe Replay (R2), 10-Session Hold (H10). Other frozen sizing books are attribution references, not extra model searches.

**Budget: 240 elapsed minutes total, including tests/reports/push. Approximately 70% directed/shared work (168 minutes), at most 30% independent Fable-led research (72 minutes).** Reserve time to finish all started hypotheses, freeze definitions, reveal confirmation once, audit and deliver. No new discovery after the confirmation reveal.

Work only in `C:\Users\james\money_chatgpt`, remote `jamesksnyder99/money_chatgpt`. Never access `C:\Users\james\Money`. Preserve historical raw data, code/report evidence and old confirmation markers. No live trades, subscription/account changes or exposed credentials.

Arrow 008 is the historical baseline reference, Arrow 009 freezes monthly reporting, and Arrow 010 supplies the fixed-dollar/equity-scaled causal sizing comparison. Do not execute their old next-step instructions. Capital constraints and redeployment are parked by the user's explicit choice. Do not begin Arrow 012.

**Scientific emphasis:** distinguish sizing-neutral stock returns, fixed-dollar contribution and compounded-account contribution. Distinguish information known at signal close from additional information known strictly before the next-session order. Post-entry paths and winner/loser labels are outcomes, never predictors. Separate the two HALF-sizing causes. Keep all eight slots of every cohort, including all-win/all-loss cohorts and documented open obligations.

**Discovery freedom:** generate/refine hypotheses and test meaningful independent ideas within the 30% lane. No preferred answer, black-box sweep, coefficient-significance filter or production promotion. Find counterexamples. Record every tested branch. Use odd signal months for discovery and one frozen even-month internal-confirmation batch; these months are not pristine OOS. Finish the full-year retrospective narrative after the reveal, with new observations labeled post-reveal.

**Parallelism / worker policy:** inspect CPU/logical cores and memory, use the largest practical worker count that improves wall-clock efficiency, and avoid competing writers/oversubscription. ThetaData acquisition, where authorized by the arrow, never exceeds eight concurrent requests across the entire process tree or a lower observed limit. Other sources obey their own rate limits. Independent feature/evidence work may be parallel; freeze decisions, dependent merges and reveal steps remain serial. Log choices and bottlenecks.

**Credentials:** confirm `git check-ignore .env` at start and before every push. Never print, log, echo, stage, commit, copy into reports or expose credential values. Use existing authorized local sources only within the active scope.

**Data integrity:** new enrichment needs timestamp/identity/action/unit/coverage checks; certification of the original ledger does not certify a new feature. No inferred split factors, future-dated predictors, stale prices labeled verified, missing features interpreted as favorable values, or manual rank-9 substitution. Exact acquisition/feature failures stay explicit. A material inherited defect blocks affected claims, not permission to quietly rewrite the baseline.

**Calendar:** nominal Wednesday signal; if closed, roll backward to the most recent exchange session; entry is the first valid exchange session after the signal. Preserve actual early closes and Hn exchange-session counting.

**Readable names first:** Winner-Fade Short; Equal-Dollar Short (PARENT); Volume-Sized Short (R4); Momentum+Volume-Sized Short (R5); Historical-Selection Replay (R1); Corrected-Universe Replay (R2); 10-Session Hold (H10); Hold-Length Ladder (H1–H10). Preserve legacy filenames/IDs for provenance.

**Monthly reporting:** apply `cg_lab_monthly_account_reporting_v1` to every economic account comparison: all months, P&L, return %, ending equity, summary counts, exact reconciliation and explicit boundary/runoff treatment. No hypothetical 10%/30% borrow haircuts in headline tables; use the active arrow's concise cost/execution footnote.

The repository is PUBLIC. Detailed datasets/CSVs under `data/` and `handoff/outgoing/` stay local and ignored. Publish reviewed code, tests, safe aggregates, schemas and provenance manifests only. Inspect staged paths AND contents before every push.

Commit/push reviewed results, verify remote equality, report exact local private deliverable paths and stop for ChatGPT audit.
