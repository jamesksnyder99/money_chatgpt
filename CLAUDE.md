# ARROW 014 ACTIVE — A13 AUDIT ACCEPTED FOR GATED TRANSITION

Arrow 013 is complete for its acquisition/generic-integrity mission and has passed ChatGPT audit for transition into Arrow 014. Preserve its honest terminal status `HOLDOUT_DATA_PARTIALLY_CERTIFIED`: the source/calendar/universe/minute layer is complete and pristine, but two evidence layers remain blocking until Arrow 014 closes them — documented dated corporate actions and point-in-time security identity/ticker mapping.

**Current assignment: `docs/CG_BUILD_ARROW_014.md`. Executor: Opus in Claude Code.** Read both `docs/CG_BUILD_ARROW_014_ADDENDUM_1.md` and `docs/CG_BUILD_ARROW_014_ADDENDUM_2.md`; the addenda control where more specific.

Do not calculate any pristine-period strategy performance merely because Arrow 014 is active. Its two-lock sequence is mandatory.

## Mandatory two-lock order

1. Verify the audited A13 source/calendar/universe state without outcomes.
2. Commit/push the immutable Arrow 014 reveal specification, exact cohort calendar, formulas, models, horizons, metrics and output schemas **before** generating holdout selected memberships.
3. Mechanically generate memberships only to identify the exact corporate-action, identity, feature, causal-entry and H8/H9/H10 lifecycle corridors requiring certification. Still no trade outcomes, P&L, hit rates, drawdowns, monthly returns or ending equity.
4. Resolve all material corridors using documented dated evidence. Price discontinuity remains triage, never proof. Rerank mechanically if any normalization changes inputs and iterate until no unresolved observation can change a frozen scored cell.
5. Complete the August 2025 prior-use/pristine-status audit.
6. Commit the exact status `HOLDOUT_DATA_CERTIFIED_FOR_PRISTINE_REVEAL` with unresolved material exceptions affecting the frozen scored cells = 0.
7. **Only then** run the entire predeclared reveal matrix in one batch.

If step 6 cannot pass, stop with exact blockers and **no performance**.

---

# Claude Code — money_chatgpt

Read `AGENTS.md`, `docs/CHATGPT_LAB_PROTOCOL.md`, `docs/SUCCESS.md`, `docs/CG_BUILD_ARROW_014.md`, `docs/CG_BUILD_ARROW_014_ADDENDUM_1.md`, and `docs/CG_BUILD_ARROW_014_ADDENDUM_2.md` before acting.

Arrow 014 is the **gated pristine-year reveal** for September 2024 through August 2025. It is a one-batch replication test, not a new optimization cycle.

## Frozen holdout definition

- August 2024 = warmup only; zero signal ownership.
- September 2024 through August 2025 = twelve pristine OOS signal months.
- August 2025 = final OOS signal month, not warmup.
- September 2025 may support late-August H8/H9/H10 lifecycle/runoff only.
- Every complete account starts fresh at $100,000 before the first September 2024 cohort.
- Expected weekly cohort count is 52 under the frozen Wednesday/holiday-rollback rule, but prove the actual calendar rather than forcing 52.

## A13 audit facts to preserve

- 1,079,750 / 1,079,750 expected newly acquired minute partitions landed across all 250 new-acquisition sessions.
- August 2025 was authenticated for reuse; September 2025 is lifecycle support only.
- The overall certified calendar corridor is 292 sessions from 2024-08-01 through 2025-09-30.
- Final acquisition run reported zero vendor errors.
- Declared structural minute-bar integrity checks found zero defects.
- A13 scored/revealed no Winner-Fade strategy outcome.
- A13's only declared remaining evidence blockers are dated corporate actions and point-in-time identity/ticker mapping. Arrow 014 may discover additional selected-corridor blockers; if so, fail closed.
- The A13 report phrase `292 of 250 new-acquisition sessions` is wording only; do not repeat it. Distinguish 250 newly acquired sessions from the authenticated/reused support that brings the overall corridor to 292.

## Frozen reveal matrix

Principal causal-preorder cells:

- Volume-Sized Short (R4), fixed-dollar: H8, H9, H10.
- Momentum+Volume-Sized Short (R5), fixed-dollar: H8, H9, H10.
- Momentum+Volume-Sized Short (R5), Arrow-010 equity scaling: H8, H9, H10.
- Rank-One 1.50x reallocation (C1), equity-scaled: H8, H9, H10.
- Equal-Dollar H10 fixed-dollar diagnostic control.
- C1 Rank-One 1.50x fixed-dollar H10 diagnostic.
- Arrow-012 C2 and C3 at H10 only, fixed-dollar and equity-scaled, as falsification controls.

No new horizon, multiplier, filter, substitution threshold, sizing rule, timing rule or model may be added after the reveal freeze is committed.

## Required outputs

Publish full account economics; all twelve monthly account rows under `cg_lab_monthly_account_reporting_v1` for **every scored complete account**; public-safe performance for every weekly cohort; rank-one/ranks-2–8 attribution; the frozen full rank-1-through-rank-8 diagnostic from Addendum 2; H8→H9→H10 attribution; drawdown episodes; historical-versus-pristine exact-cell comparisons; C2/C3 falsification outcomes; and the frozen Arrow-011 mechanism confirmation panel. Use each equity-scaled book's own causal account path.

## Data integrity

Historical Data Certification remains fail-closed. Preserve immutable vendor landing/provenance. Full ranking-field completeness, point-in-time identity/type, documented corporate actions, split-consistent price/share-volume units, R5 feature windows, causal pre-order observations and H8/H9/H10 lifecycle data must be certified for every observation capable of altering a scored cell. A jump screen is triage, not action evidence. Missing data are never zero/favorable, never a halt, and never a reason to substitute the next rank.

Use the official installed ThetaData SDK and existing Stocks Professional entitlement only where additional verification/retrieval is required. Do not launch Theta Terminal, change subscriptions, buy data, use options or start sub-minute ingest. Total Theta concurrency is at most eight across the process tree or any lower observed vendor/session limit. Preserve the Arrow 013 fail-closed/session-supervisor lessons.

## Authentication / credentials

Confirm `git check-ignore -v .env` at start and before every push. Never print, echo, serialize, stage, commit or expose credentials. Silent empty-success responses are failures.

## Calendar

Nominal Wednesday signal; if closed, roll backward to the most recent exchange session. Entry is the first valid exchange session after signal. H8/H9/H10 count exchange sessions after actual fill. Preserve actual early closes, New York timezone and DST.

## Research governance

- Preserve all Arrow 001–013 artifacts unchanged.
- No positive result substitutes for verification.
- Do not alter rules after the reveal manifest is committed.
- Do not run post-OOS optimization on this period.
- Component results never substitute for complete-account economics.
- Equity-scaled books use their own causal signal-close equity, not another book's path.
- Historical references may be computed after freeze only under the exact same rule and cannot change the reveal.
- The rank-by-rank table is a diagnostic only; do not create a top-five book, drop ranks 6–8, or change C1 from it on this holdout.

## Public/private safety

Work only in `C:\Users\james\money_chatgpt`; never access `C:\Users\james\Money`. The repository is public. Raw vendor data, selected-symbol details, detailed trade rows, identity/action evidence and daily ledgers stay in ignored `data/` / `handoff/outgoing/` as required. Public outputs contain reviewed aggregates, schemas, hashes and safe cohort economics. Inspect staged filenames and contents before every push.

Use human-readable strategy names first. Headline economics use the standing commission/spread convention; do not add generic 10%/30% borrow haircuts. Use the concise excluded-cost/availability footnote.

If the certification gate cannot close, stop with exact blockers and **no performance**. If it closes, finish the entire predeclared matrix before stopping. Commit/push reviewed outputs, verify remote equality, report private deliverables and stop for ChatGPT audit. Do not begin a post-reveal optimization arrow.
