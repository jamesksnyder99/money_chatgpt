# Claude Code — money_chatgpt

Read `AGENTS.md`, `docs/CHATGPT_LAB_PROTOCOL.md`, `docs/SUCCESS.md`, and the explicitly assigned CG arrow before acting.

**Current assignment: `docs/CG_BUILD_ARROW_012.md`. Executor: Opus in Claude Code.**

Arrow 012 is a narrow **A11 measurement repair + whole-engine challenger** study. First repair Arrow 011's analysis layer without changing any frozen trade/account result. Then score exactly three predeclared challenger books against the incumbent Momentum+Volume-Sized Winner-Fade Short (R5), Corrected-Universe Replay (R2), 10-Session Hold (H10), causal pre-order quantities: (C1) rank-one 1.50x relative allocation funded by ranks 2–8 at unchanged cohort base capital, (C2) deterministic off-high substitution from original ranks 9–20 while protecting rank one and preserving cohort base capital, and (C3) the combination. The incumbent C0 remains protected.

**CRITICAL HOLDOUT EMBARGO:** the user is acquiring an additional historical year for later pristine confirmation. Arrow 012 may score only the frozen 52 cohorts from September 2025 through August 2026. Do not inspect, score, summarize, rank, feature-engineer, or learn outcomes from new signal cohorts outside that set even if new files appear locally. Existing lookbacks/runoff needed by the frozen 52 are permitted. The active arrow requires a hard cohort-membership guard.

Starting research checkpoint before Arrow 012: Arrow 011 completion commit `e1701d21331fb8b2cc4ecd1741776cab75e23b25`.

Planning allocation: target 75–90 elapsed minutes, hard stop 120 minutes inclusive of tests/reports/push. Finish the core work rather than opening unrelated research.

Work only in `C:\Users\james\money_chatgpt`, remote `jamesksnyder99/money_chatgpt`. Never access `C:\Users\james\Money`. Preserve historical raw data, code/report evidence and old confirmation markers. No live trades, subscription/account changes or exposed credentials.

Arrow 008 is the certified historical baseline reference; Arrow 009 freezes monthly reporting; Arrow 010 supplies the fixed-dollar/equity-scaled R5 comparison; Arrow 011 is an anatomy study whose original artifacts must remain byte-identical. Do not execute old arrows' next-step instructions.

**Repair rule:** analytical repairs may change Arrow 012's interpretation of A11 relationships, but may not alter frozen selections, quantities, trade P&L, daily accounts, or prior artifacts. Row permutation/ties must not create spurious within-cohort effects. Behavioral leakage tests must challenge actual data/time/action behavior rather than only inspect source strings.

**Challenger rule:** component improvements count only if the complete chronological engine improves. Build C0/C1/C2/C3 independently. Cohort base capital is equalized as specified in Arrow 012; equity-scaled books use each challenger's own causal signal-close equity path. Do not add isolated gains together. Preserve interaction failures. No fourth challenger, leverage sweep, hold optimization, timing grid, redeployment, long model, or new threshold after scoring.

**Candidate verification:** rank 9–20 replacement candidates are new research observations. Verify identity, eligibility, action/feature units, causal pre-order entry observation, and H10 lifecycle before they can affect economics. Data failure is not a market filter: do not silently keep the original, skip to the next replacement, or treat missingness as favorable.

**Parallelism / worker policy:** inspect CPU/logical cores and memory, use practical concurrency without competing writers. ThetaData acquisition, where authorized by the arrow, never exceeds eight concurrent requests across the entire process tree or a lower observed vendor limit. Keep causal equity paths serial within each book. Log worker choices and bottlenecks.

**Credentials:** confirm `git check-ignore .env` at start and before every push. Never print, log, echo, stage, commit, copy into reports or expose credential values.

**Calendar:** nominal Wednesday signal; if closed, roll backward to the most recent exchange session; entry is the first valid exchange session after the signal. Preserve actual early closes and Hn exchange-session counting.

**Readable names first:** Winner-Fade Short; Equal-Dollar Short (PARENT); Volume-Sized Short (R4); Momentum+Volume-Sized Short (R5); Historical-Selection Replay (R1); Corrected-Universe Replay (R2); 10-Session Hold (H10); Hold-Length Ladder (H1–H10). Preserve legacy filenames/IDs for provenance.

**Monthly reporting:** apply `cg_lab_monthly_account_reporting_v1` to every complete account comparison: all months, P&L, return %, ending equity, summary counts, exact reconciliation and boundary/runoff treatment. Do not headline generic borrow haircuts; use the standing concise execution-cost footnote.

The repository is PUBLIC. Detailed datasets/CSVs under `data/` and `handoff/outgoing/` stay local and ignored. Publish reviewed code, tests, safe aggregates, schemas and provenance manifests only. Inspect staged paths AND contents before every push.

Commit/push the required challenger freeze before scoring, then final reviewed results. Verify remote equality, report exact private deliverable paths, and stop for ChatGPT audit. Do not begin the future-year reveal.